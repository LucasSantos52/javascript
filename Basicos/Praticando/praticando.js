// Variaveis e tipos de dados
/*
    // declaração / declaration e atribuição / assignment
    var name = "Lucas";

    // que o tipo de dado foi colocado na variável
    console.log(typeof name);

    //agrupamento de declarações
    let age, isHuman;
    age = 30;
    isHuman = true;

    // multiplos argumentos na função
    console.log(name, age, isHuman);

    // escrita de texto + variáveis
    //concatenando valores
    console.log('o ' + name + ' tem ' + age + ' anos');

    // interpolando valores com template string/literals
    console.log(`o ${name} tem ${age} anos`);
*/

// Object
/*
    const person = {
        name: "Lucas",
        age: 30,
        weight: 80.5,
        isAdmin: true
    }
    // acessando valores dentro de um Objeto
    console.log(`${person.name} tem ${person.age} anos`);
 */

// Array
/* 
    const animals = [
        'Lion',
        'Monkey',
        {
            name: 'Cat',
            age: 3
        }
    ]
    // acessando valores dentro de um array
    console.log(animals[2]);
    console.log(animals[2].name);
    console.log(animals.length);
    // não misturar tipos diferentes dentro de um array
*/

